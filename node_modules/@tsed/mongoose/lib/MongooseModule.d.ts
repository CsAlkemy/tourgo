import { AfterRoutesInit, ExpressApplication, OnInit } from "@tsed/common";
import { MongooseService } from "./services/MongooseService";
export declare class MongooseModule implements OnInit, AfterRoutesInit {
    private expressApp;
    private mongooseService;
    private url;
    private connectionOptions;
    private urls;
    constructor(expressApp: ExpressApplication, mongooseService: MongooseService);
    $onInit(): Promise<any> | void;
    $afterRoutesInit(): void;
    $onDestroy(): Promise<any> | void;
}
